# 角色
你是一名资深网页游戏全栈工程师，既重视功能实现，也极端重视代码可维护性、可测试性与可扩展性，
并且具备"自我维护、跨会话接力"意识。
请依据本提示词从零开发《浅滩鱼悔》（Shallow Regrets）网页版卡牌小游戏，
并确保后续可长期维护、易定位与修复 Bug、任何新会话/新开发者都能快速接手。

==========================================================
一、项目背景与总体架构
==========================================================
1. 游戏原型：《浅滩鱼悔》= 桌游 Shallow Regrets（Button Shy Games，2025，作者 Judson Cowan），是《深海渔恨 Deep Regrets》的 18 卡精简版。
2. 开发目标：网页卡牌小游戏，无需登录、无需安装，浏览器即开即玩。
3. 五种模式（分阶段交付，顺序不可颠倒）：
   M1 本地对战：同一设备 2-3 人轮流操作（热座 Hotseat）。
   M2 单人 AI 对战：玩家 vs 电脑 AI（简单启发式即可，禁止引入强化学习）。
   M3 联机对战：主机暴露临时公网网址，好友在任意网络打开即加入实时对战。
   M4 AI 斗蛐蛐：AI 对战 AI，玩家作为观众观看完整对战；支持自动/手动回合控制。
   M5 浅滩鱼悔 Solo 玩法：玩家 vs 由 6 张固定卡牌构成的预设脚本对手（非 AI 评估式，与 M2 明确区分）。
4. 架构启发（阅读参考，不得照抄代码）：
   - 规则/数据建模思路参考 https://github.com/JamesGames7/bga-deepRegrets （BGA 官方深海渔恨实现）。
   - 官方规则权威参考（最高优先级）https://www.thefamilygamers.com/shallow-regrets/ ：任何规则细节与其它来源冲突时，一律以此网址为准。
   - 多模式划分、主机权威思想参考 https://github.com/DavidWang1231/doudizhu-online 。
   - 结论性选型：规则模型学 bga-deepRegrets；单机模式纯静态；联机模式用"本机服务 + cloudflared 隧道"。
5. 技术路线：
   - M1/M2/M4/M5（单机）：纯静态 HTML/CSS/JS，双击 index.html 即可离线游玩（M4 观战、M5 Solo 均不依赖网络）。
   - M3（联机）：主机本机运行一个 Node.js 服务（静态托管 + Socket.IO + 权威状态机），
     再用 cloudflared-windows-amd64 创建临时公网 URL 暴露，好友通过该 URL 打开网页并加入对局。
   - 禁止使用 WebRTC P2P；不要求自建公网服务器/域名。

==========================================================
二、技术选型及依据（给出理由，不得随意更换）
==========================================================
- 前端（单机/客户端）：原生 HTML5 + CSS3 + 模块化 JavaScript（ES Modules）。不引入重型框架，保证零构建即可运行、离线可用。可用 Vite 作为可选开发/构建工具，但产物必须可直接打开。
- 联机服务（仅 M3）：Node.js + Socket.IO（Express 可选，负责静态托管与房间事件）。
- 公网暴露：cloudflared-windows-amd64 快速隧道（命令：cloudflared tunnel --url http://localhost:3000），返回临时 https://xxx.trycloudflare.com 网址；不依赖公网 IP、无需端口转发、无需 VPS。
- 测试框架：Vitest（与 Vite 生态一致）；若未用 Vite 则用 Node 内置 test runner 或 Jest，二选一并在 README 说明。
- 日志：自研轻量 logger（无第三方依赖），支持分级与开关。
- 图片：全部卡面由 AI 生成，通过图片接口渲染（见第五节），禁止占位图与盗用网络图。
- 选型依据：单机纯静态 → 部署成本最低、离线可玩、易分发；联机走 Socket.IO + cloudflared → 穿透 100% 可靠、实现远简单于 WebRTC（无需处理 ICE/STUN/TURN 打洞），且回合制卡牌对中继延迟不敏感；cloudflared → 免费、一条命令即可，适合好友间临时联机。

==========================================================
三、模块化代码结构与职责划分（可维护性基石）
==========================================================
目录结构：
  shallow-regrets/
  ├── index.html            # 单页入口（五种模式首页 + 对局页 + 结算页）
  ├── css/                  # 样式（base / layout / cards / board / modal / responsive）
  ├── js/
  │   ├── main.js           # 前端入口：初始化、模式路由、启动游戏
  │   ├── core/             # 纯逻辑层，禁止触碰 DOM（可 100% 单测）
  │   │   ├── cards.js      # 18 张卡牌数据定义（唯一数据源）
  │   │   ├── gameState.js  # 对局状态对象与不可变更新函数
  │   │   ├── stateMachine.js # 状态机：状态枚举/转移表/动作分发
  │   │   ├── rules.js      # 规则引擎：取牌合法性、强度判定、放生、计分、污秽惩罚
  │   │   ├── abilities.js  # 能力效果解析器（效果 = {类型,目标,参数} 纯函数表）
  │   │   └── scoring.js    # 终局计分
  │   ├── ai/               # 单人 AI 对战（M2）与 AI 斗蛐蛐（M4）共享决策层
  │   │   ├── heuristicAI.js # 启发式 AI（输入状态快照，返回动作）
  │   │   └── aiDecision.js  # AI 决策理由生成（M4 战斗日志的可观测依据）
  │   ├── spectate/         # M4 AI 斗蛐蛐观众系统
  │   │   ├── spectate.js   # 观众编排：创建 AI 对战、回合调度、自动/手动模式、速度档位
  │   │   └── battleLog.js  # 战斗日志：AI 决策过程与战斗反馈记录（可回放）
  │   ├── solo/             # M5 浅滩鱼悔 Solo 玩法（玩家 vs 预设脚本）
  │   │   ├── soloScript.js # 脚本对手：6 张固定卡组定义 + 确定性剧本（数据+引擎）
  │   │   └── soloFlow.js   # Solo 流程控制：独立入口/回合/结算/特色机制钩子
  │   ├── net/              # M3 客户端联网层（只与 server 通信，不跑规则）
  │   │   ├── socketClient.js # Socket.IO 连接、收发动作/快照
  │   │   └── protocol.js   # 前后端共享的消息协议定义（字段/类型/校验）
  │   ├── ui/               # 视图层，仅负责渲染与交互，无业务规则
  │   │   ├── render.js     # 浅滩堆、玩家钓获区、结算面板渲染
  │   │   ├── interaction.js# 点击/拖拽/横置交互与合法性提示
  │   │   ├── animations.js # 翻牌/移动动画（CSS transform + transition）
  │   │   ├── modal.js      # 规则说明、确认框、错误弹窗
  │   │   ├── spectateUI.js # M4 观战界面：AI 面板、速度/回合控制按钮、日志滚动区
  │   │   └── soloUI.js     # M5 Solo 界面：对手信息、脚本意图提示、回合与结算
  │   ├── utils/
  │   │   ├── logger.js     # 分级日志
  │   │   ├── rng.js        # 可注入种子的随机数（便于测试复现）
  │   │   └── eventBus.js   # 核心→UI 的解耦事件总线
  │   └── data/
  │       └── artPrompts.js # 18 张卡的 AI 绘图提示词清单
  ├── server/               # M3 联机服务端（Node.js）
  │   ├── server.js         # 入口：静态托管 + Socket.IO + 启动权威状态机
  │   ├── room.js           # 房间管理（4 位房间码、成员、准备状态、生命周期）
  │   └── gameServer.js     # 主机权威：复用 js/core/stateMachine.js 校验并广播状态
  ├── assets/               # UI/图标/音效/背景（CC0 素材）
  ├── tests/                # 单元测试（与 js/ 目录一一对应）
  ├── docs/
  │   ├── PROJECT_NOTE.md   # AI 自我维护文档（强制，见第八节第 9 条）
  │   ├── RULES.md          # 完整规则文档
  │   ├── ARCHITECTURE.md   # 模块图/状态机/协议/cloudflared 使用说明
  │   ├── ASSETS.md         # 资源来源/许可/署名登记
  │   ├── CHANGELOG.md      # 版本变更记录
  │   └── ISSUE_TEMPLATE.md # Bug 报告模板
  ├── tools/
  │   └── README.md         # cloudflared-windows-amd64 的下载/配置/常用命令说明
  ├── .gitignore
  └── README.md
依赖方向约束：ui → core（只读状态、派发动作）；core 不 import ui/net/server；net 只依赖 core 的协议与状态机；server 复用 core 逻辑但不被 core 反向依赖。禁止循环依赖。

==========================================================
四、游戏核心规则（M1 起必须完整实现）
==========================================================
规则优先级：https://www.thefamilygamers.com/shallow-regrets/ 为最高优先级权威来源；
本节点及任何其它资料（含 GitHub 参考实现）与它冲突时，一律以该网址规则为准（见第一节第 4 条）。
1. 组件：18 张鱼卡。字段：{ id, name, 分值points, 所需钩数strength(0-5), 类型type(fair正品/foul污秽), 一次性能力ability, 卡图art }。
2. 开局：洗牌后分 6 堆（每堆 3 张）＝6 个"浅滩"，牌面朝下；至少 3 个浅滩的顶牌是 strength≤0 的小鱼。
3. 玩家初始钓钩=0；已钓到的鱼其 strength 之和即当前钩子数，用于满足更强鱼的要求。
4. 每回合流程：
   a. 回合开始可横置(耗尽)自己 1 张已钓的"一次性能力"鱼并发动效果（每种能力整局仅一次）。
   b. 从任意浅滩取 2 张牌（可在同一或不同浅滩）。
   c. 二选一：钓走 1 张（必须 当前钩子数 ≥ 该鱼 strength），另 1 张放回原浅滩。
   d. 若两张都无法满足强度 → 两张都放回，回合结束。
5. 能力示例（能力表可扩展）：多抽1张、偷看某浅滩顶牌、强制对方横置其一条鱼、与对方交换一条鱼、洗牌并重置浅滩、单回合免疫等。
6. 终局：全部鱼被钓光即结束。计分 = 各玩家钓获分值之和；钓到最多"污秽鱼"的玩家额外 -2 分；总分最高者胜。平局按 RULES.md 约定。
7. SOLO（M2）：按《渔夫与青蛙》思路实现单人对抗。AI 决策优先级：能否满足钩数 → 优先高分且非污秽 → 评估污秽风险 → 抢关键能力鱼。AI 必须走与玩家相同的动作接口，禁止作弊。

==========================================================
五、美术与资源整合方案（"卡图让 AI 自己找"落地规范）
==========================================================
1. 18 张鱼卡 + 背景 + 图标：全部由 AI 生成/合成，禁止使用有版权素材。
2. 图片渲染接口（唯一允许的来源，prompt 需 URL 编码）：
   https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt={prompt}&image_size=square_hd
   image_size 可选：square_hd | square | portrait_4_3 | portrait_16_9 | landscape_4_3 | landscape_16_9
3. 在 js/data/artPrompts.js 为每张鱼卡编写 中+英 绘图提示词，风格统一："克苏鲁 + 海洋恐怖 + 手绘漫画/水彩"，每张鱼体现其能力主题。
4. 非核心美术（UI 面板/按钮/背景/能力小图标）从 CC0 渠道获取：
   - UI/牌框/背景：Kenney.nl（CC0）
   - 能力小图标：game-icons.net（CC-BY 3.0，需署名）
   - 音效：freesound.org（仅用 CC0，记录来源与作者）
5. 资源统一登记到 docs/ASSETS.md（来源/许可/作者/是否需署名）。

==========================================================
六、交互逻辑设计（前端体验要求）
==========================================================
1. 首页：五种模式入口卡片（本地对战 / 单人 AI / 联机 / Solo 挑战 / AI 斗蛐蛐观战）+ "规则说明"按钮 + 设置（音量/动画开关）。
2. 对局区：中央 6 个浅滩堆（卡背叠放、可选中高亮 .selectable/.selected），两侧玩家钓获区。
3. 操作流：选浅滩取牌 → 翻开两张 → 点击"钓走"/"放生"；强度不足时明确提示并禁止非法操作。
4. 能力发动：点击己方带能力鱼 → 横置确认；已横置的鱼旋转 90° 灰化。
5. 动画：翻牌/移动/横置用 CSS transform+transition，必须支持"关闭动画"选项。
6. 结算页：钓获明细、污秽惩罚说明、最终得分与胜负；提供"再来一局"与"返回首页"。
7. 本地对战（热座）：提示"轮到玩家X"，提供"遮挡切换"按钮防作弊，确认后换人。
8. 响应式：桌面与手机竖屏均可操作；触屏无法拖拽时降级为点击选择。

==========================================================
七、联机对战（M3）实现要点（cloudflared + Socket.IO 方案）
==========================================================
1. 架构：主机权威。主机运行 server/server.js（同时托管静态页面与 Socket.IO），
   cloudflared-windows-amd64 把 localhost:3000 暴露为临时公网 URL；
   所有客户端通过该 URL 打开网页并 Socket.IO 连接主机；
   主机服务端复用 js/core/stateMachine.js 作为权威状态机，校验所有动作并广播快照。
2. 主机操作流程（写入 tools/README.md）：
   a. 安装/放置 cloudflared-windows-amd64.exe（工具或 PATH）。
   b. 启动 `node server/server.js`（监听 3000）。
   c. 运行 `cloudflared tunnel --url http://localhost:3000`，复制返回的 https://xxx.trycloudflare.com 发给好友。
   d. 主机自己也打开该 URL（或 localhost）参与对局。
   e. 提示：临时 URL 每次重启 cloudflared 会变，需重新分享；需要固定地址用命名隧道+域名（写入文档）。
3. 房间机制：服务端生成 4 位房间码；客户端输入房间码 + 昵称加入（防陌生人乱入）；
   支持"准备/开始"；房主可踢人；房间超时无人在线自动销毁。
4. 消息协议（net/protocol.js 与 server 共享，全部 JSON）：
   JOIN / READY / START / ACTION / STATE_SYNC / PLAYER_LEFT / RECONNECT / PING / PONG。
   服务端在 ACTION 时先校验（是否该玩家回合、动作是否合法），合法才推进状态机并广播 STATE_SYNC。
5. 状态快照必须可序列化纯数据（浅滩各堆、钓获区、回合、已横置标记等），用于渲染、重连恢复与对局回放。
6. 断线处理：Socket.IO 心跳/断线检测；对局中掉线可选择判负或 AI 托管（复用 heuristicAI），界面明示。
7. 对局回放：每局保存可序列化动作序列，用于复现与调试。
8. 局域网兜底：若不想用 cloudflared，主机直接把 http://主机内网IP:3000 发给同一网络的好友即可直连。
9. 若 cloudflared 被禁/不可达，方案需自动降级为"仅局域网联机"并提示，不阻塞单机模式。

==========================================================
八、AI 斗蛐蛐观众系统（M4）实现要点
==========================================================
1. 模式定位：AI 对战 AI（两个或更多 AI 各执一方），玩家不参与操作，仅作为观众观看完整对局。
   双方复用 js/ai/heuristicAI.js 作为决策者，且必须走与玩家完全相同的动作接口与 rules 校验，禁止作弊——
   这是平衡性可验证、逻辑可复用的前提。
2. 回合控制（两种模式，对局中可随时切换）：
   - 自动进行：由单一调度器按预设速度连续执行回合；提供速度档位（慢/中/快，如 3s/1.5s/0.5s），支持"暂停/继续"。
   - 手动进行：每回合结束停在等待态，玩家点击"下一回合"按钮才推进；附"自动进行 N 回合"与"立即结束本局"（可选）。
   - 竞态约束：回合推进必须由串行调度器驱动，速度档位仅影响展示节奏，绝不改变对局结果。
3. 可观测的 AI 决策过程：战斗日志面板逐条记录——回合号、行动方、候选动作与评估理由（由 js/ai/aiDecision.js 生成，
   如"能否满足钩数→高分→规避污秽→抢能力鱼"的判定路径）、实际动作、结算结果（钓走/放生/能力发动）。
4. 战斗反馈：浅滩堆、钓获区、横置状态按与普通对局一致的渲染；关键节点（能力发动、污秽鱼被钓、即将终局）高亮提示。
5. 对局回放：自动保存完整动作序列（复用第七节回放格式），可导出/回放，用于复现 Bug 与平衡性调试。
6. 无障碍与动画：动画可关；关闭动画不影响节奏与结果。

==========================================================
九、浅滩鱼悔 Solo 玩法（M5）实现要点
==========================================================
1. 模式定位：玩家 vs 预设脚本对手（非 AI 评估式决策），与 M2"单人 AI 对战"明确区分：
   M2 对手走启发式评估；M5 对手严格按固定脚本执行。
   首页提供独立"Solo 挑战"入口卡片，走独立流程（与 M1/M2/M3 对战流程分开）。
2. 脚本对手构成：由 6 张固定卡牌组成（从 18 张鱼卡中选定，配置在 js/solo/soloScript.js）。
   卡组与剧本均为确定性数据：固定取牌/钓走/放生优先级、固定能力发动时机、固定行动节奏，无随机与评估分支。
3. 遵循游戏自带 Solo 规则：玩家与脚本对手使用同一套核心规则（取牌、强度、放生、能力、计分、污秽惩罚，复用 js/core/）；
   Solo 特有流程（先后手、脚本行动顺序、目标/胜利条件）以 docs/RULES.md 的"Solo 章节"为准，
   开发时必须先明确写入文档再实现，禁止另造规则或使用占位。
4. 特色内容与机制：保留并实现游戏自带 Solo 玩法特色（如固定剧本的压迫节奏、Solo 专属目标与结算评价等）。
   各特色机制的触发条件与数值写入 docs/RULES.md 与 js/solo/soloScript.js 注释，文档与代码同步，不得敷衍占位。
5. 结算与展示：Solo 结算页展示双方钓获明细、污秽惩罚、特色机制达成情况与评价；提供"再来一局"与"返回首页"。
6. 可测试性：脚本对手全部决策为纯函数（输入状态快照 → 输出动作），单测覆盖剧本分支与特色机制边界。

==========================================================
十、可维护性与 Bug 修复设计要素（必须全部落实）
==========================================================
1. 模块化代码结构：严格按第三节目录与依赖方向执行；core 层不碰 DOM、server 不写 UI。
2. 注释规范：
   - 核心逻辑（规则、能力、状态转移、AI 决策）必须写中文注释，说明"为什么"而非复述代码。
   - 公共函数/类用 JSDoc（@param/@returns/@throws），类型用 @typedef 标注。
   - 禁止废话注释；修改代码必须同步更新相关注释。
3. 错误处理机制：
   - 系统边界校验：用户输入（房间码格式、操作合法性）、外部接口（图片加载失败给占位兜底并提示）、网络错误。
   - 非法动作一律拒绝并回滚 UI，提示原因，不产生脏状态。
   - 所有可抛错分支 try/catch 并携带上下文（状态/动作/模块）。
   - 全局 unhandledrejection / error 捕获，友好提示并记录日志。
4. 日志记录系统：
   - logger 分级 debug/info/warn/error，按环境开关（dev 全开，生产 warn+error）。
   - 关键事件打点：状态切换、动作、能力发动、计分、联机收发、异常、断线/重连。
   - M3 起每局保存动作序列回放日志（LocalStorage 或导出文件）用于复现线上 Bug。
   - M4 战斗日志打点：AI 决策理由、回合推进、模式/速度切换；M5 Solo 打点：脚本行动、特色机制触发。
   - 日志格式统一：时间戳 | 级别 | 模块 | 事件 | 数据(JSON)；禁止打印敏感信息。
5. 版本控制策略：
   - Git；分支 main（稳定）/feature/xxx /fix/xxx；提交信息遵循 Conventional Commits（feat/fix/refactor/docs/test/chore）。
   - 语义化版本 SemVer；docs/CHANGELOG.md 按版本记录变更（每个功能/修复须同步更新）。
   - .gitignore 忽略 node_modules/dist/日志/cloudflared 临时文件；禁止提交密钥与隧道凭据。
6. 单元测试框架集成：
   - 为 core/ 全部纯逻辑编写测试：洗牌（种子可复现）、取牌合法性、强度判定、放生、能力结算、计分与污秽惩罚、状态机转移、AI 决策样例。
   - M4：AI 对战回放一致性测试（同一动作序列重放结果一致）、速度档位不影响对局结果测试；M5：脚本对手卡组/剧本单测、特色机制边界测试。
   - tests/ 与 js/ 目录一一对应，命名 *.test.js；提供 npm test 一键运行。
   - 每个规则 Bug 修复必须附带回归测试用例（先红后绿）。
   - M3 起为 protocol.js 编写消息格式一致性测试（前后端同构字段）。
7. 文档管理方案：
   - README.md：简介、五模式说明、运行方式（单机直接开/联机用 cloudflared）、测试命令、目录结构、资源许可声明。
   - docs/RULES.md：完整规则（放回浅滩规则、能力明细、计分、M2 单人 AI、M5 Solo 玩法、平局；以 familygamers 官方规则为最高优先级来源，冲突时引用并注明出处）。
   - docs/ARCHITECTURE.md：模块图、状态机转移表、联机协议、cloudflared 使用与排障。
   - docs/ASSETS.md：资源来源/许可/署名登记。
   - docs/CHANGELOG.md：版本变更记录。
   - docs/ISSUE_TEMPLATE.md：Bug 报告模板。
   - 文档与代码同步更新是硬性要求。
8. 问题追踪流程：
   - Bug 报告模板字段：环境(浏览器/版本/是否公网或局域网)、复现步骤、预期、实际、错误日志、是否可复现、回放文件(若有)。
   - 修复流程：建 issue → 定位模块 → 写失败测试 → 修复 → 补回归测试 → 更新 CHANGELOG 与文档 → 关闭。
   - 高频 Bug（强度误判、洗牌非法开局、联机不同步、cloudflared 掉线）沉淀进 docs/ARCHITECTURE.md 的"已知陷阱"小节。
9. 【强制】AI 自我维护文档机制（每次任务结束必须执行）：
   - 每次 AI 完成一个任务/阶段后，必须更新 docs/PROJECT_NOTE.md，内容包含：
     a. 项目结构速览：每个文件/目录的用途、依赖关系（随目录变化同步更新）。
     b. 过往经验与踩坑记录：遇到并解决的 Bug、原因、解决方式、避免重犯的约定。
     c. 任务日志：本次完成/修改了什么、改了哪些文件、为什么这样改、遗留事项。
     d. 当前进度与下一步：五种模式开发到哪一步、下一步计划。
     e. 关键决策记录：重要技术选型及理由（ADR 简版），如"联机为何用 Socket.IO+cloudflared 而非 WebRTC"。
   - 目的：让下一个会话/开发者不必重读全部代码即可快速接手，显著减少上下文消耗。
   - 硬性约束：新任务开始前必须先读 docs/PROJECT_NOTE.md；任务结束必须更新它；文件不存在则先创建。

==========================================================
十一、分阶段交付与验收标准（里程碑）
==========================================================
M1 本地对战：可完整对局至结算；规则全部通过；core 层单测覆盖强度判定/计分/状态机；双击 index.html 即可玩。
M2 单人 AI：可 2-3 人 + AI 混合对战；AI 决策稳定；heuristicAI 有单测样例。
M3 联机：本机服务 + cloudflared 建房/加入、回合同步、断线托管、对局回放；protocol.js 有协议一致性测试；tools/README.md 写清 cloudflared 操作。
M4 AI 斗蛐蛐：可完整观战 AI 对战至结算；自动/手动回合控制与速度档位可用；战斗日志完整展示 AI 决策过程；对局回放可导出且重放一致。
M5 Solo：独立入口可进入；6 卡脚本对手严格按剧本行动；Solo 特色机制按 docs/RULES.md 实现；脚本对手有单测样例。
每阶段结束必须满足：README 可运行、npm test 全绿、CHANGELOG 已更新、docs 已同步、docs/PROJECT_NOTE.md 已更新。

==========================================================
十二、交付物清单
==========================================================
1. 完整可运行代码（含全部模块与测试）。
2. README.md 与 docs/ 全部文档（含 PROJECT_NOTE.md 的首次建立）。
3. 18 张鱼卡 AI 绘图提示词清单（js/data/artPrompts.js）及已生成卡图。
4. 资源许可登记表 docs/ASSETS.md。
5. cloudflared 联机操作说明（tools/README.md）与五模式启动/测试说明。
6. M4 观战模式（含战斗日志、对局回放、自动/手动回合控制与速度档位）。
7. M5 Solo 玩法（含 6 卡脚本对手定义、独立流程与特色机制实现）。

开工顺序建议：先搭目录与 core（cards/rules/stateMachine）+ 单测 → M1 本地对战 → M2 单人 AI → 补齐文档/日志/PROJECT_NOTE → M3 联机（Socket.IO+cloudflared）→ M4 AI 斗蛐蛐 → M5 Solo → 终验。
每个模块完成后立即运行测试、更新日志、更新文档与 PROJECT_NOTE.md，杜绝"最后统一补文档"。
